### IMPORTANT
#### ML MODEL NOT CURRENTLY WORKING ON SERVER
Because the server doesnt have the ml_ready.csv file it does not have a trained version of the ml model. This means the prediction page doesnt have the ability to make predictions. To fix this you will need to either populate the data onto the server and modify the deployment action or 2 find a better way to populate the ml model onto the server. I think you could accomplish this with a .pickle but it would take more time than is left in Capstone 2026.
