services:
  mcp-api:
    build: .
    container_name: mcp-api
    ports:
      - "8000:8000"
    environment:
      # 可选：如果你确认该字段存在才打开，否则留空
      # ORDER_BY_FIELDS: "FireDiscoveryDateTime DESC"
      ORDER_BY_FIELDS: ""
    restart: unless-stopped
